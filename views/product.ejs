<!DOCTYPE html>
<html lang="en">

<head>
    <title>اطلب الان</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="shortcut icon" type="image/x-icon" href="/img/<%= smd_[6].link %>">

    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/templatemo.css">
    <link rel="stylesheet" href="/css/custom.css">

    <!-- Load fonts style after rendering the layout styles -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;200;300;400;500;700;900&display=swap">
    <link rel="stylesheet" href="/css/fontawesome.min.css">

    <!-- Slick -->
    <link rel="stylesheet" type="text/css" href="/css/slick.min.css">
    <link rel="stylesheet" type="text/css" href="/css/slick-theme.css">
    <link href="https://fonts.cdnfonts.com/css/ds-arabic" rel="stylesheet">
<!--
    
TemplateMo 559 Zay Shop

https://templatemo.com/tm-559-zay-shop

-->
</head>

<body>
    
    <!-- Start Top Nav -->
    <nav class="navbar navbar-expand-lg bg-dark navbar-light d-none d-lg-block" id="templatemo_nav_top">
        <div class="container text-light">
            <div class="w-100 d-flex justify-content-between">
                <div>
                    <i class="fa fa-phone mx-2"></i>
                    <a class="navbar-sm-brand text-light text-decoration-none" href="tel:010-020-0340"><%= smd_[7].link %></a>
                </div>
                <div>
                    
                <%
                smd_.forEach(smd=>{
                    if ( smd.link !== "" && (smd.name == "tiktok" || smd.name == "facebook" || smd.name == "instagram" || smd.name == "youtube") ) {
                %>
                <a class="text-light" href="<%= smd.link %>" target="_blank"><i class="fab fa-<%= smd.name %> fa-sm fa-fw"></i></a>
                <%
                    }
                })
                %>
                </div>
            </div>
        </div>
    </nav>
    <!-- Close Top Nav -->

    

    <!-- Header -->
    <nav class="navbar navbar-expand-lg navbar-light shadow">
        <div class="container d-flex justify-content-between align-items-center">

            <a class="navbar-brand text-success logo h1 align-self-center" href="/">
                <img src="/img/<%= smd_[4].link %>" alt="" srcset="">
            </a>

            <div id="container">
                
                <div id="cursor"></div>
                <div id="text"></div>
                <div id="text">الشحن</div>
            </div>


        </div>
    </nav>
    <!-- Close Header -->

    <!-- Modal -->
    <div class="modal fade bg-white" id="templatemo_search" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg" role="document">
            <div class="w-100 pt-1 mb-5 text-right">
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form action="" method="get" class="modal-content modal-body border-0 p-0">
                <div class="input-group mb-2">
                    <input type="text" class="form-control" id="inputModalSearch" name="q" placeholder="Search ...">
                    <button type="submit" class="input-group-text bg-success text-light">
                        <i class="fa fa-fw fa-search text-white"></i>
                    </button>
                </div>
            </form>
        </div>
    </div>


    <%
    var item1, tags1, idd, taggs_s
    prrs.forEach((item)=>{
        
        var imgs = item.image.split(" aissa ")
        var newImgTxt = ''
        var newImg = []

        imgs.forEach((img)=>{
            newImgTxt += img.replace('publicmyimagesprr', '/myimages/prr/') + " j "
            newImg = newImgTxt.split(" j ")
        })
        
        if(item.id == id){
            item1 = item.name_of_pr.split(",")
            idd = item.id
            var tags1 = item.name_of_pr.split(',')
            taggs_s = tags1

            if(item.online == "true")
            {

        
    %>


    <!-- Open Content -->
    <section class="bg-light">
        <div class="container pb-5">
            <div class="row">
                <div class="col-lg-5 mt-5">
                    <div class="card mb-3">
                        <img class="card-img img-fluid" src="<%= newImg[0] %>" alt="Card image cap" id="product-detail">
                    </div>
                    <div class="row">
                        <!--Start Controls-->
                        <%var aissaXX1=0;
                         if( newImg.length>3 && aissaXX1 == 0 ){ aissaXX1++; %>
                            <div class="col-1 align-self-center">
                                <a href="#multi-item-example" role="button" data-bs-slide="prev">
                                    <i class="text-dark fas fa-chevron-left"></i>
                                    <span class="sr-only">Previous</span>
                                </a>
                            </div>
                        <% } %>
                        <!--End Controls-->
                        <!--Start Carousel Wrapper-->
                        <div id="multi-item-example" class="col-10 carousel slide carousel-multi-item" data-bs-ride="carousel">
                            <!--Start Slides-->
                            <div class="carousel-inner product-links-wap" role="listbox">

                                
                            <%
                            var x=0;
                            if(newImg.length%3 != 0){ x = 1 }
                            for ( let i = 0;i < parseInt(newImg.length/3 + x);i++) 
                            {
                                if( newImg[i*3+0] !== "" ){
                            %>
                                <div class="carousel-item">
                                    <div class="row">
                                        <%
                                            if ( i*3+0 < newImg.length && newImg[i*3+0] !== "" )
                                            {
                                        %>
                                        <div class="col-4">
                                            <a href="#">
                                                <img class="card-img img-fluid" src="<%= newImg[i*3] %>" alt="Product Image 1">
                                            </a>
                                        </div>
                                        <%}%>
                                        <%
                                            if ( i*3+1 < newImg.length && newImg[i*3+1] !== "" )
                                            {
                                        %>
                                        <div class="col-4">
                                            <a href="#">
                                                <img class="card-img img-fluid" src="<%= newImg[i*3+1] %>" alt="Product Image 1">
                                            </a>
                                        </div>
                                        <%}%>
                                        <%
                                            if ( i*3+2 < newImg.length && newImg[i*3+2] !== "" )
                                            {
                                        %>
                                        <div class="col-4">
                                            <a href="#">
                                                <img class="card-img img-fluid" src="<%= newImg[i*3+2] %>" alt="Product Image 1">
                                            </a>
                                        </div>
                                        <%}%>
                                    </div>
                                </div>
                                
                            <%
                                }
                            }
                        %>

                            </div>
                            <!--End Slides-->
                        </div>
                        <!--End Carousel Wrapper-->
                        <!--Start Controls-->
                        <%var aissaXX=0;
                         if( newImg.length>3 && aissaXX == 0 ){ aissaXX++; %>
                            <div class="col-1 align-self-center">
                                <a href="#multi-item-example" role="button" data-bs-slide="next">
                                    <i class="text-dark fas fa-chevron-right"></i>
                                    <span class="sr-only">Next</span>
                                </a>
                            </div>
                        <% } %>
                        <!--End Controls-->
                    </div>
                </div>
                <!-- col end -->
                <div class="col-lg-7 mt-5">
                    <div class="card">
                        <div class="card-body">
                            <%
                            if ( item.price <= item.discount || item.discount == 0 ) { %>
                                <div class="prc">
                                    <h1 class="price1"><%= item.price %></h1>
                                    <p>dh</p>
                                </div>
                            <% }else{   %>
                                <div class="prc">
                                    <h3 class="price0"><%= item.price %></h3>
                                    <h1 class="price1"><%= parseInt((item.discount*item.price)/10)/10 %></h1>
                                    <p class="dh">dh</p>
                                </div>
                            <% } %>

                            <h1 class=""><%= item.title %></h1>
                            
                        <div class="description"></div>
                        <div class="testmeeeeeee__"><%= item.description %></div>
                            <br>
                            <form action="/buy/<%= item.id %>" method="post">
                                        <%
                                        var theColors = item.color_s.split(",")
                                        var allFull = false
                                        theColors.forEach((clr)=>{
                                            if ( clr !== "" ) { allFull = true }
                                        })
                                            if ( theColors.length !== 0 && allFull ) {
                                        
                                        %>


                                            <ul class="list-inline">
                                                <li class="list-inline-item">
                                                    <p class="text-muted">
                                                        <%
                                                        var did = false
                                                            
                                                        if ( !did && item.color_s !== "" ) { did = true %>
                                                        <div class="cir">
                                                            <% theColors.forEach((singleColor)=>
                                                            {
                                                                if ( singleColor !== "" ) {
                                                            %>
                                                            <p id="<%= singleColor %>"></p>

                                                        <%
                                                                }
                                                            }) 
                                                        }
                                                        %>
                                                        </div>
                                                    </p>
                                                </li>
                                                <li class="list-inline-item">
                                                    <h3> : الألوان</h3>
                                                </li>
                                            </ul>


                                        <%  } %>
                                        <% if ( item.size_s.split(' aissaoulkhir__ ').length !== 0 && item.size_s !== "" ) { %>


                                            <div class="col-auto">
                                                <ul class="list-inline pb-3">
                                                    <h3> : الأحجام
                                                        <input type="hidden" name="product-size" id="product-size" value="S">
                                                    </h3>
                                                    <%
                                                        var sz__s = item.size_s.split(' aissaoulkhir__ ')[0].split('/_aissa,aissa_/')
                                                        var prc_s = item.size_s.split(' aissaoulkhir__ ')[1].split('/_aissa,aissa_/')
                                                        
                                                    let i = 0;
                                                            var ddd = "btn btn-size btn-secondary"
                                                        sz__s.forEach((sz)=>{
                                                            if ( sz !== "" && sz !== " " ) {
                                                                
                                                    %>
                                                    <li class="list-inline-item" id="<%= prc_s[i] %>"><span class="<%= ddd %>"><%= sz %></span></li>
                                                    <%  ddd = "btn btn-success btn-size"} i++; }) %>
                                                    <%

                                                    %>
                                                </ul>
                                            </div>


                                        <% } %>
                                        <div class="col-auto">
                                            <ul class="list-inline pb-3">
                                                <li class="list-inline-item"><span class="btn btn-success" id="btn-minus">-</span></li>
                                                <li class="list-inline-item"><span class="badge bg-secondary" id="var-value">1</span></li>
                                                <li class="list-inline-item"><span class="btn btn-success" id="btn-plus">+</span></li>
                                                <li class="list-inline-item text-right">
                                                    الكمية
                                                    <input type="hidden" name="product_quanity_aissa" id="product-quanity" value="1">
                                                </li>
                                            </ul>
                                        </div>
                                <hr>
                                

                                <div>
                                    <input type="text" name="name" class="name" placeholder="الاسم" required>
                                    <br>
                                    <input type="text" name="phone_num" class="phone_num" placeholder="رقم الهاتف" required>
                                    <br>
                                    <input type="text" name="adress" class="adress" placeholder="العنوان" required>
                                    
                                                <input type="text" name="type_size" class="type_size" >
                                                <input type="text" name="type_color" class="type_color" >
                                                <input type="text" name="price_cc" class="price_cc" >
                                </div>
                                <br>
                                <div class="row pb-3">
                                    <div class="col d-grid">
                                        <button type="submit" class="btn btn-success btn-lg byyyyuyuy" name="submit" value="buy">اطلب الان</button>
                                    </div>
                                </div>
                            </form>

                        </div>
                    </div>
                </div>

                <%
                if ( item.video !== "nothing here" ) {
                    var vids = item.video.split(' aissa ')  
                    
                    vids.forEach((vid)=>{

                        if ( vid[vid.length - 1] == "4" ) {
                  %>
                    <video class="vid_aissa" width="320" height="240" controls>
                        <source src="<%= vid.replace('publicmyimagesprr', '/myimages/prr/') %>" type="video/mp4">
                    </video>
                <%
                        }
                    })
                }
                %>

            </div>
        </div>
<%
        taggs_s.forEach(tag=>{
%>
            <a class="tags" href="/category/<%= tag %>"><%= tag %></a>
<%
        })
%>
<br>
<br>
    </section>
    <!-- Close Content -->

    <%
            }
        }
    }) %>
    <!-- Start Article -->
<%
    if (similarProducts[0].image !== "aissa")
    {
%>
    <section class="py-5">
        <div class="container">
            <div class="row text-left p-2 pb-3">
                <h4>منتجات ذات صلة</h4>
            </div>
            <div id="carousel-related-product">
<%
        const weight = 5;
        var sortedProducts = similarProducts.sort(() => Math.random() - 0.5);
        sortedProducts = sortedProducts.sort((a, b) => {
            return (b.sold_ * weight) - (b.view_s) - (a.sold_ * weight) + (a.view_s);
        });
        sortedProducts = sortedProducts.sort((a, b) => {
            if (b.sold_ !== a.sold_) {
            return b.sold_ - a.sold_;
            }
            return b.view_s - a.view_s;
        });

        var card_ = 0
        sortedProducts.forEach(S_Prr=>{
            if ( card_ < 8 && S_Prr.id != id && S_Prr.online == 'true' ){
                card_++
                var size_s = S_Prr.size_s.split(' aissaoulkhir__ ')[0].split('/_aissa,aissa_/')
                var color_s = S_Prr.color_s.split(',')
                var imgs = S_Prr.image.split(" aissa ")

%>
                <div class="p-2 pb-3">
                    <div class="product-wap card rounded-0">
                        <div class="card rounded-0">
                    <div class="hdn">
                        <img class="card-img rounded-0 img-fluid" src="/myimages/prr/<%= S_Prr.image.split(' aissa ')[0].replace('publicmyimagesprr', '') %>">
                    </div>
                            <div class="card-img-overlay rounded-0 product-overlay d-flex align-items-center justify-content-center">
                                <ul class="list-unstyled">
                                    <li><a class="btn btn-success text-white mt-2" href="/product/<%= S_Prr.id %>"><i class="far fa-eye"></i></a></li>
                                </ul>
                            </div>
                        </div>
                        <div class="card-body">
                            <a href="/product/<%= S_Prr.id %>" class="h3 text-decoration-none"><%= S_Prr.Mini_title %></a>
                            <ul class="w-100 list-unstyled d-flex justify-content-between mb-0">
                                <%
                                var sizeSlx=""
                                var slx = ""
                                    if ( size_s.length !== 0 ) {
                                        size_s.forEach(siz=>{
                                            if (siz !== "") {sizeSlx += slx + siz;
                                            slx = '/';}
                                        })
                                %>
                                <li><%= sizeSlx %></li>
                                <%  
                                        
                                    }
                                %>
                                
                                <%
                                    var did1 = false
                                        
                                    if ( !did1 && S_Prr.color_s !== "" ) { did = true %>
                                    <div class="cir out">
                                        <% color_s.forEach((singleColor)=>
                                        {
                                            if ( singleColor !== "" ) {
                                        %>
                                        <p id="<%= singleColor %>" class="g" ></p>
            
                                    <%
                                            }
                                        }) 
                                    }
                                %>
                                
                            </ul>
                            <p class="text-center mb-0"><%= S_Prr.price %></p>
                        </div>
                    </div>
                </div>




<%
            }
        })
%>

<%
    }else{
%>
<br>
<br>
<br>
<br>
<div style="text-align: center;">
    <h1>404</h1>
    <a href="/">go home</a>
</div>
<br>
<br>
<br>
<br>
<br>

<%
    }
%>

            </div>
        </div>
    </section>

    <!-- End Article -->


    <!-- Start Footer -->
    <footer class="bg-dark" id="tempaltemo_footer">
        <div class="container">
            <div class="row">

                <div class="col-md-4 pt-5">
                    
                    <a class="navbar-brand text-success logo h1 align-self-center" href="/">
                        <img src="/img/<%= smd_[5].link %>" alt="" srcset="">
                    </a>
                    <ul class="list-unstyled text-light footer-link-list">
                        <li>
                            <i class="fa fa-phone fa-fw"></i>
                            <a class="text-decoration-none" href="tel:<%= smd_[7].link %>"><%= smd_[7].link %></a>
                        </li>
                    </ul>
                </div>

                
                <div class="col-auto me-auto">
                    <ul class="list-inline text-left footer-icons">
                        <%
                        smd_.forEach(smd=>{
                            if ( smd.link !== "" && (smd.name == "tiktok" || smd.name == "facebook" || smd.name == "instagram" || smd.name == "youtube") ) {
                        %>
                        <li class="list-inline-item border border-light rounded-circle text-center">
                            <a class="text-light text-decoration-none" target="_blank" href="<%= smd.link %>"><i class="fab fa-<%= smd.name %> -f fa-lg fa-fw"></i></a>
                        </li>
                        <%
                            }
                        })
                        %>
                    </ul>
                </div>
            </div>
        </div>


    </footer>
    <!-- End Footer -->

    <!-- Start Script -->
    <script src="/js/jquery-1.11.0.min.js"></script>
    <script src="/js/jquery-migrate-1.2.1.min.js"></script>
    <script src="/js/bootstrap.bundle.min.js"></script>
    <script src="/js/templatemo.js"></script>
    <script src="/js/custom.js"></script>
    <script src="/js/aissa.js"></script>
    <!-- End Script -->

    <!-- Start Slider Script -->
    <script src="/js/slick.min.js"></script>
    <script>
        $('#carousel-related-product').slick({
            infinite: true,
            arrows: false,
            slidesToShow: 4,
            slidesToScroll: 3,
            dots: true,
            responsive: [{
                    breakpoint: 1024,
                    settings: {
                        slidesToShow: 3,
                        slidesToScroll: 3
                    }
                },
                {
                    breakpoint: 600,
                    settings: {
                        slidesToShow: 2,
                        slidesToScroll: 3
                    }
                },
                {
                    breakpoint: 480,
                    settings: {
                        slidesToShow: 2,
                        slidesToScroll: 3
                    }
                }
            ]
        });
        document.querySelectorAll('.card-img.img-fluid').forEach((img) => {
        if(img.src == "/"){
            img.remove()
        }
    })
    </script>
    <!-- End Slider Script -->

</body>

</html>